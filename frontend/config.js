/** Desenvolvimento local — frontend em :8080, API em :3100 (via proxy do dev_server). */
window.__LAV60_CONFIG__ = {
  /** Vazio = mesma origem (proxy /api no dev_server). Em produção, use a URL pública da API. */
  apiBase: '',
};
